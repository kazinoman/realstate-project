export const homeUrl = "/";

export const profileUrl = {
  profile: `${homeUrl}profile`,
  advertisements: `${homeUrl}profile/my-advertisements`,
  requests: `${homeUrl}profile/my-requests`,
  addProperty: `${homeUrl}profile/add-new-property`,
  listings: `${homeUrl}profile/listings`,
  edit: `${homeUrl}profile/edit`,
};
